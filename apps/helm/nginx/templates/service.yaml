apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-runtime.fullname" . }}
  labels:
    {{- include "nginx-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nginx-runtime.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
